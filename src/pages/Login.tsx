import LoginCard from '../components/_login/LoginCard';

export default function LoginPage() {
    return (
        <div className="bg-slate-100 min-h-screen overflow-x-hidden py-28 flex justify-center items-center">
            <LoginCard />
        </div>
    );
}